{% set version = "20200121" %}

package:
  name: pdfminer.six
  version: {{ version }}

source:
  url: https://github.com/pdfminer/pdfminer.six/archive/{{ version}}.tar.gz
  sha256: 643639ec0bcd46751da167c16a512bb1608c4999028f320fe1b09c60b63288f3

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python >=3
    - pip
  run:
    - python >=3
    - six
    - pycryptodome
    - sortedcontainers
    - chardet

test:
  imports:
    - pdfminer

  commands:
    - pdf2txt.py -h
    # Can't test dumppdf.py b/c -h raises issues.
    # Can't test latin2ascii.py unless you feed it input.

about:
  home: https://github.com/pdfminer/pdfminer.six
  license_file: LICENSE
  license: MIT
  license_family: MIT
  summary: 'PDF parser and analyzer'
  dev_url: https://github.com/pdfminer/pdfminer.six

extra:
  recipe-maintainers:
    - pmlandwehr
    - pvanheus
